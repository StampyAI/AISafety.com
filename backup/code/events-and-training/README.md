# Old /events-and-training page code

The combined Events & training page that the /events + /training split replaced.
Kept for reference only – it is not routed (next.config.ts permanently
redirects /events-and-training to /training) and not compiled (backup/ is
excluded in tsconfig.json). The page rendered the legacy "Events & training"
Airtable table (tblx0L8qJEaLBxJFS) through three iframe embeds.
